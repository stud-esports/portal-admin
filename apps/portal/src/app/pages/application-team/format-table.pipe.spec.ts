import { FormatTablePipe } from './format-table.pipe';

describe('FormatTablePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTablePipe();
    expect(pipe).toBeTruthy();
  });
});
